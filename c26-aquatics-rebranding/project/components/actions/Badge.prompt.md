Uppercase pill label — location tags, stat units, category eyebrows.

```jsx
<Badge tone="teal">Year-round</Badge>
<Badge tone="orange">New</Badge>
```

Tones: `neutral`, `navy`, `teal`, `orange`, `outline`, `outline-light`. Sizes `sm` / `md`.
