Styled dropdown matching Input. For booking widgets (program / age / location).

```jsx
<Select label="Program" options={["Swim Lessons","Competitive","Triathlon"]} />
```

`options` accepts strings or `{value,label}` objects.
