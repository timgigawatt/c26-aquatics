Labelled text input with teal focus ring.

```jsx
<Input label="Email" type="email" placeholder="you@email.com" hint="We'll confirm your slot here." />
```

Passes through all native `<input>` props (`type`, `placeholder`, `value`, `onChange`…).
